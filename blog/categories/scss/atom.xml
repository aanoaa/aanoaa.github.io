<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: scss | Mustache :{]]></title>
  <link href="http://aanoaa.github.com/blog/categories/scss/atom.xml" rel="self"/>
  <link href="http://aanoaa.github.com/"/>
  <updated>2013-12-20T01:11:59+09:00</updated>
  <id>http://aanoaa.github.com/</id>
  <author>
    <name><![CDATA[Hyungsuk Hong]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[sass &amp; scss]]></title>
    <link href="http://aanoaa.github.com/blog/2011/12/17/sass-and-scss/"/>
    <updated>2011-12-17T00:08:00+09:00</updated>
    <id>http://aanoaa.github.com/blog/2011/12/17/sass-and-scss</id>
    <content type="html"><![CDATA[<p><code>oops.scss</code> 파일을 아래처럼 맨들고,</p>

<p>```scss oops.scss
@import "compass/css3";</p>

<h1>header,#footer {</h1>

<pre><code>margin: 0 auto;
</code></pre>

<p>}
.error {</p>

<pre><code>@include border-radius(5px);
background-color: #DD0000;
.important {
    border-width: 3px;
}
</code></pre>

<p>}
```</p>

<p>콤퓨타로 뭔가를 하면,</p>

<p>```bash
$ sass --watch oops.scss:oops.css</p>

<p>or</p>

<p>$ compass watch /path/to/     # 끝판왕임 저는 이렇게 씀</p>

<p>or</p>

<h1>emacs scss-mode 쓰면 그냥 저장함과 동시에 변환할 수 있는데 compass</h1>

<h1>의 기능은 사용할 수 없음</h1>

<p>```</p>

<p><code>oops.css</code> 파일을 맨들어 줍니다.</p>

<p>```css oops.css
/<em> line 2, ../sass/oops.scss </em>/</p>

<h1>header, #footer {</h1>

<p>  margin: 0 auto;
}</p>

<p>/<em> line 5, ../sass/oops.scss </em>/
.error {
  -moz-border-radius: 5px;
  -webkit-border-radius: 5px;
  -o-border-radius: 5px;
  -ms-border-radius: 5px;
  -khtml-border-radius: 5px;
  border-radius: 5px;
  background-color: #DD0000;
}
/<em> line 8, ../sass/oops.scss </em>/
.error .important {
  border-width: 3px;
}
```</p>

<p>이처럼 <a href="http://sass-lang.com/">sass</a>는 <code>.sass</code> 또는 <code>.scss</code>를 <code>.css</code>로 맨들어 줍니다.</p>

<p>장점으로는 <code>oops.scss</code>에서 <code>@include border-radius(5px);</code> 를 보듯이
함수와 변수를 사용할 수 있다는 것입니다. 코드의 중복을 막아주고(Don't
Repeat Yourself), 셀렉터들의 계층을 두어서 쉽게 작성할 수 있습니다.
<code>compass</code>를 함께 사용하면 이미 흔하게 사용되고 있는 선조들의 <code>best
practice</code>를 <code>import</code> 해서 쓸 수 있습니다.</p>

<h1>설치 및 사용</h1>

<p>```bash
$ gem install sass compass    # 저는 compass도 씀
$ compass create /path/to/Your-Awesome-Project/</p>

<h1>config.rb 를 수정하고, stylesheets, sass, images 등의 경로를</h1>

<h1>프로젝트 환경에 맞게 바꿔줍니다. rails 라면 필요없어 보입니다.</h1>

<p>$ compass watch .    # config.rb 있는 곳을 지정해 줘야 합니다.</p>

<h1>scss 파일을 수정하면 css 파일에도 반영됩니다.</h1>

<p>```</p>

<p>저는 <code>config.rb</code> 를 다음과 같이 변경해서 사용중입니다.</p>

<p><code>rb config.rb
http_path = "/"
css_dir = "static/css"
sass_dir = "static/sass"
images_dir = "static/images"
javascripts_dir = "static/scripts"
</code></p>

<h1>그밖에..</h1>

<p>emacs <a href="http://www.emacswiki.org/emacs/ScssMode">scss-mode</a>를 사용하면 watcher 를 띄우지 않고도
저장과 동시에 <code>css</code>파일을 맨들 수 있습니다.</p>

<p><code>
(add-to-list 'load-path "~/.emacs.d/vendor/scss-mode")
(require 'scss-mode)
</code></p>

<p><a href="http://blueprintcss.org/">blueprint</a>와 같은 grid framework 와 함께 사용할 수
있습니다. 저는 그렇게 쓰고 있습니다.</p>

<p><code>octopress</code> 도 <code>scss</code>사용합니다.</p>

<h1>결론</h1>

<p><code>ruby</code> 모듈이자 툴입니다. 저희 회사는 <code>perl</code>로 개발합니다만 물론
사용할 수 있습니다. 너무나도 훌륭한 오픈소스 입니다.</p>

<p>훌륭한 개발환경을 가지고 있고, 프로그래밍에는 능숙하지만, <code>css</code>만
잡으면 한없이 약해지는 분들이라면 간단한 <code>scss</code>문법을 공부하고
<code>compass</code>툴 사용법을 익혀서 써먹으면 좋을 것 같습니다.</p>
]]></content>
  </entry>
  
</feed>
